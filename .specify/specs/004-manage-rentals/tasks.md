# Tarefas — Gerenciar Locações

- [x] Criar `pages/rent-property.html` com formulário validado
- [x] Exibir contexto da propriedade selecionada (imagem, título, endereço)
- [x] Implementar `js/rentals.js` com `createRental` e consistência de estado
- [x] Alterar o estado da propriedade para `alugada` ao registrar uma locação ativa
- [x] Validar campos obrigatórios e datas antes de persistir os dados
- [x] Exibir confirmação prévia e mensagem de sucesso/erro
- [x] Substituir o campo de entrada `tenantName` por um menu suspenso (dropdown) de inquilinos existentes
- [x] Persistir `tenantId` em vez de `tenantName` em cada locação
- [x] Alertar o usuário se a propriedade selecionada já possuir um contrato ativo